
spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:postgresql://localhost:5432/cibtest
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: passtest
    hikari:
      idle-timeout: 10000